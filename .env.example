ANTHROPIC_API_KEY=sk-ant-...
GCP_PROJECT_ID=dw-onfly-prd
BIGQUERY_LOCATION=us-central1
BIGQUERY_MAX_BYTES_BILLED=10737418240
# Opcional: caminho para service account JSON (se não usar gcloud auth)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Google OAuth SSO
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# Restringe login a um domínio (ex: onfly.com.br). Vazio = qualquer email Google
ALLOWED_EMAIL_DOMAIN=
